"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const useBoolean_1 = require("../useBoolean");
const useCounter_1 = require("../useCounter");
const useInterval_1 = require("../useInterval");
function useCountdown({ seconds, interval, isIncrement, }) {
    const { count, increment, decrement, reset: resetCounter, } = (0, useCounter_1.useCounter)(seconds);
    const { value: running, setTrue: start, setFalse: stop } = (0, useBoolean_1.useBoolean)(false);
    const reset = () => {
        stop();
        resetCounter();
    };
    (0, useInterval_1.useInterval)(isIncrement ? increment : decrement, running ? interval : null);
    return [count, { start, stop, reset }];
}
exports.default = useCountdown;
//# sourceMappingURL=useCountdown.js.map